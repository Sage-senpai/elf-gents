# deps
/node_modules
/.pnp
.pnp.js
# this project uses pnpm — ignore stray npm/yarn lockfiles
package-lock.json
yarn.lock

# next
/.next/
/out/
next-env.d.ts

# production
/build

# env — never commit keys
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo

# receipts written during local runs
/.receipts

# private notes / registration answers — never commit (may hold key hints)
*.local.md
/_private/
